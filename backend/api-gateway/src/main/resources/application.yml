server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "https://fintrack-liart.vercel.app"
              - "http://localhost:3000"
            allowedMethods: [GET, POST, PUT, DELETE, PATCH, OPTIONS]
            allowedHeaders: "*"
            allowCredentials: true
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin

      routes:
        # ================= AUTH (NO AUTH REQUIRED) =================
        - id: users-service-auth
          uri: ${USERS_SERVICE_URL:http://users-service:8081}
          predicates:
            - Path=/api/auth/**
          filters:
            - RewritePath=/api/auth/(?<segment>.*), /api/auth/${segment}

        # ================= NOTIFICATIONS (NO AUTH FOR TESTING) =================
        - id: notifications-service
          uri: ${NOTIFICATIONS_SERVICE_URL:http://notifications-service:8086}
          predicates:
            - Path=/api/notifications/**
          filters:
            - AuthenticationFilter

        # ================= USERS (REQUIRES AUTH) =================
        - id: users-service
          uri: ${USERS_SERVICE_URL:http://users-service:8081}
          predicates:
            - Path=/api/users/**
          filters:
            - AuthenticationFilter
            - RewritePath=/api/users/(?<segment>.*), /api/users/${segment}

        # ================= TRANSACTIONS (REQUIRES AUTH) =================
        - id: transactions-service
          uri: ${TRANSACTIONS_SERVICE_URL:http://transactions-service:8082}
          predicates:
            - Path=/api/transactions/**
          filters:
            - AuthenticationFilter
            - RewritePath=/api/transactions/(?<segment>.*), /api/transactions/${segment}

        - id: transactions-service-recurring
          uri: ${TRANSACTIONS_SERVICE_URL:http://transactions-service:8082}
          predicates:
            - Path=/api/recurring-transactions/**
          filters:
            - AuthenticationFilter
            - RewritePath=/api/recurring-transactions/(?<segment>.*), /api/recurring-transactions/${segment}

        # ================= BUDGETS (REQUIRES AUTH) =================
        - id: budgets-service
          uri: ${BUDGETS_SERVICE_URL:http://budgets-service:8085}
          predicates:
            - Path=/api/budgets/**
          filters:
            - AuthenticationFilter

        # ================= ALERTS (REQUIRES AUTH) =================
        - id: alerts-service
          uri: ${ALERTS_SERVICE_URL:http://alerts-service:8083}
          predicates:
            - Path=/api/alerts/**
          filters:
            - AuthenticationFilter
            - RewritePath=/api/alerts/(?<segment>.*), /api/alerts/${segment}

        # ================= REPORTS (REQUIRES AUTH) =================
        - id: reports-service
          uri: ${REPORTS_SERVICE_URL:http://reports-service:8084}
          predicates:
            - Path=/api/reports/**
          filters:
            - AuthenticationFilter
            - RewritePath=/api/reports/(?<segment>.*), /api/reports/${segment}

# Application-specific configuration
app:
  environment: ${APP_ENVIRONMENT:docker}
  dev:
    bypass-auth: ${DEV_BYPASS_AUTH:false}
    default-user-id: ${DEV_USER_ID:dev-user-123}

# JWT (must match users-service)
jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: ${JWT_EXPIRATION:86400000}

# Management/Actuator
management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info,metrics,prometheus,gateway
  endpoint:
    health:
      show-details: always
      show-components: always
    gateway:
      enabled: true

# Logging
logging:
  level:
    com.fintrack: INFO
    org.springframework.cloud.gateway: INFO
    reactor.netty: INFO