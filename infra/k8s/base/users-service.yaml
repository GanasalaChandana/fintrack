apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: fintrack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      containers:
      - name: users-service
        image: fintrack-users-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8081
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "development"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres:5432/fintrack"
        - name: SPRING_DATASOURCE_USERNAME
          value: "fintrack_user"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "fintrack_pass"
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: fintrack
spec:
  type: NodePort
  selector:
    app: users-service
  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30081
